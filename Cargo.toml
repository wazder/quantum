[workspace]
resolver = "2"
members = [
    "crates/quantum-core",
    "crates/quantum-runtime",
    "crates/quantum-loader",
    "crates/quantum-jit",
    "crates/quantum-ntdll",
    "crates/quantum-kernel32",
    "crates/quantum-cli",
]

[workspace.package]
version = "0.0.1"
edition = "2024"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
repository = "https://github.com/wazder/quantum"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[workspace.lints.clippy]
# Default clippy groups (correctness, suspicious, complexity, perf, style).
# Pedantic is gated behind `cargo clippy -- -W clippy::pedantic` for the
# polish pass; it would drown out real findings if on by default tonight.
missing_safety_doc = "deny"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"

[profile.dev]
opt-level = 1
